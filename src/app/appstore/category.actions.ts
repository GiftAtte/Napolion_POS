import { createAction } from '@ngrx/store';

export const createCategory = createAction('[CategoryForm Component] Create');


export const updateCategory = createAction('[CategoryForm Component] Update');

export const loadCategory = createAction('[Category Component] Load');

export const deleteCategory = createAction('[Category Component] Delete');

